import { effectFirebaseClient } from './effect-firebase-client.js';

describe('effectFirebaseClient', () => {
  it('should work', () => {
    expect(effectFirebaseClient()).toEqual('effect-firebase-client');
  });
});
