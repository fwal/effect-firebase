import { effectFirebaseMock } from './effect-firebase-mock.js';

describe('effectFirebaseMock', () => {
  it('should work', () => {
    expect(effectFirebaseMock()).toEqual('effect-firebase-mock');
  });
});
