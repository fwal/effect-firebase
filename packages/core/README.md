# effect-firebase

Firebase services for Effect.

## Building

Run `nx build effect-firebase` to build the library.

## Running unit tests

Run `nx test effect-firebase` to execute the unit tests via [Vitest](https://vitest.dev/).
