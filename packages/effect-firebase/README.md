# effect-firebase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build effect-firebase` to build the library.

## Running unit tests

Run `nx test effect-firebase` to execute the unit tests via [Jest](https://jestjs.io).
